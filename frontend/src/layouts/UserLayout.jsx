// frontend/src/layouts/UserLayout.jsx
// Now a thin wrapper — all structure lives in AppLayout.
import AppLayout from './AppLayout';

export default function UserLayout() {
  return <AppLayout />;
}
